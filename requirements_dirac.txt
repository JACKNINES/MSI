# MSI Pipeline — Dependencies for the dirac server (CUDA-capable)
# Install: pip install -r requirements_dirac.txt

# Core scientific stack
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
statsmodels>=0.14
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.65
joblib>=1.3
Pillow>=10.0

# Cheminformatics
rdkit>=2023.09

# Mol2Vec (semantic 2D embeddings)
gensim>=4.3
mol2vec>=0.2

# Uni-Mol 3D embeddings
unimol-tools>=0.1
# unimol-tools pulls torch as a dependency but on a CUDA server you may want
# to install torch explicitly first against the CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
